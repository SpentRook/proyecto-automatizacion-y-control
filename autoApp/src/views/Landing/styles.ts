import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    display: "flex",
    rowGap: 20,
  },
  containerChart: {
    display: "flex",
    rowGap: 5,
  },
});

export { styles };
