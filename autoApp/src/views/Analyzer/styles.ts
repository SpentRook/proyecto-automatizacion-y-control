import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  btnsContainer: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
  },
});

export { styles };
