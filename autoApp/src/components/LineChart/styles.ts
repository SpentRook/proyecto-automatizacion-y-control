import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: "100%",
    borderColor: "#E4D9BF",
    borderWidth: 2,
    borderRadius: 8,
    padding: 4,
  },
});

export { styles };
