import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    padding: 12,
    display: "flex",
    alignItems: "center",
    backgroundColor: "#E4D9BF",
    marginVertical: 4,
    borderRadius: 8,
  },
  contentContainer: {
    display: "flex",
    gap: 5,
    alignItems: "center",
    justifyContent: "center",
    flexDirection: "row",
  },
});

export { styles };
